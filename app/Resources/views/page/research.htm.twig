{% extends 'base.htm.twig' %}
{% block title %}Research{% endblock %}
  {% block breadcrumb %}
      <li class="rsmItem sfBreadcrumbNavigation">
          <div class="rsmTemplate">
              <a id="ctl00_cphBreadCrumb_T84CFA523010_ctl00_ctl00_Breadcrumb_i0_hypBreabcrumb" href="{{ path('homepage') }}">Home</a>

          </div>
          <span class="sfBreadcrumbNodeSeparator"></span></li>
      <li class="rsmItem sfNoBreadcrumbNavigation select">
          <div class="rsmTemplate">

              <a id="ctl00_cphBreadCrumb_T84CFA523010_ctl00_ctl00_Breadcrumb_i1_hypBreabcrumb">Research</a>

          </div>
      </li>
  {% endblock %}
{% block banner %}{% endblock %}
{% block body %}
    <style>
        .bread-crumb-control{
            margin-top: 75px;
        }
    </style>
    <h1>Research</h1>


    <div class="sfNavWrp sfNavHorizontalWrp ">

            {% for category in categories %}
                <div class="portal-item col-sm-12">
                    <div class="portal-item-border border-none">
                        <div class="description" style="min-height: 0px; max-height: none;">
                            <h2 class="portal-title-size"><a href="{{ path('reports', {'slug': category.slug}) }}"><span class="{{ category.icon }}">&nbsp;</span>{{ category.categoryName }}</a></h2>
                            <p>{{ category.summary }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}

    </div>

{% endblock %}